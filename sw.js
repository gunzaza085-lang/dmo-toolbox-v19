const CACHE='gun-shop-dmo-v20-2-admin-shell-performance-5';const NAVIGATION_NETWORK_TIMEOUT_MS=4000;const SHELL=['./','./index.html','./app.css?v=20260910-v20.2-admin-shell-performance-5','./app.js?v=20260910-v20.2-admin-shell-performance-5','./config.js?v=20260910-v20.2-admin-shell-performance-5','./manifest.webmanifest','./icon-192.png','./icon-512.png'];self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting())));self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));self.addEventListener('fetch',event=>{const request=event.request,url=new URL(request.url);if(request.method!=='GET'||url.origin!==location.origin)return;const network=fetch(request);if(request.mode==='navigate'){const cached=caches.match('./index.html').catch(()=>undefined),timeout=new Promise(resolve=>setTimeout(()=>resolve(cached),NAVIGATION_NETWORK_TIMEOUT_MS));event.waitUntil(network.then(response=>response&&response.ok?caches.open(CACHE).then(cache=>cache.put('./index.html',response.clone())):undefined).catch(()=>{}));event.respondWith(Promise.race([network,timeout]).then(response=>response||network).catch(()=>cached));return;}event.waitUntil(network.then(response=>response&&response.ok?caches.open(CACHE).then(cache=>cache.put(request,response.clone())):undefined).catch(()=>{}));event.respondWith(caches.match(request).then(hit=>hit||network).catch(()=>caches.match('./index.html')));});
